
module Mantle.Prelude (
    module Prelude,
) where

import Prelude hiding (
    (&&), (||), not,
    (==), (/=),
    (<), (>), (<=), (>=),
    min, max,
    (+), (-), (*), (/),
    fromInteger,
    read)

